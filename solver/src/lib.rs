//! This file is a workaround for the issue that
//! you can't run `cargo build` when there doesn't exist either
//! `src/lib.rs` or `src/main.rs`; even if `src/main.rs` is generated
//! during the build.
